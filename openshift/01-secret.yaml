# =============================================================
# openshift/01-secret.yaml
# Holds all sensitive credentials.
#
# Values here are BASE64-encoded placeholders.
# Replace them with real values before applying:
#
#   echo -n 'mysecretvalue' | base64
#
# Or let deploy.sh substitute them automatically.
# =============================================================
apiVersion: v1
kind: Secret
metadata:
  name: paygate-secret
  namespace: paygate
  labels:
    app.kubernetes.io/part-of: paygate
type: Opaque
data:
  # echo -n 'payment_gateway' | base64
  db-name: cGF5bWVudF9nYXRld2F5
  # echo -n 'pguser' | base64
  db-user: cGd1c2Vy
  # echo -n 'REPLACE_ME_DB_PASSWORD' | base64  ← replace before apply
  db-password: UkVQTEFDRV9NRV9EQl9QQVNTV09SRA==
  # echo -n 'REPLACE_ME_ROOT_PASSWORD' | base64
  db-root-password: UkVQTEFDRV9NRV9ST09UX1BBU1NXT1JE
  # echo -n 'REPLACE_ME_DJANGO_SECRET_KEY' | base64
  django-secret-key: UkVQTEFDRV9NRV9ESkFOR09fU0VDUkVUX0tFWQ==
